{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Default permissions for the NEXUS desktop shell loading window.",
  "windows": ["main"],
  "permissions": ["core:default"]
}
