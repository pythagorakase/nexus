#!/bin/bash
# Wrapper script for IR evaluation tool (PostgreSQL version)

SCRIPT_DIR="$( cd "$( dirname "${BASH_SOURCE[0]}" )" &> /dev/null && pwd )"
cd "$SCRIPT_DIR"

echo "Launching NEXUS IR Evaluation Tool (PostgreSQL version)..."
python -m ir_eval.ir_eval "$@"