{
  "column:assets.new_story_creator.seed_initial_mystery": "Current cache writer clears this legacy field to NULL; intended historical semantics are not established by a live reader.",
  "column:assets.new_story_creator.seed_potential_allies": "Current cache writer clears this legacy field to NULL; intended historical semantics are not established by a live reader.",
  "column:assets.new_story_creator.seed_potential_obstacles": "Current cache writer clears this legacy field to NULL; intended historical semantics are not established by a live reader.",
  "column:assets.new_story_creator.seed_starting_location": "Current cache writer clears this legacy field to NULL; intended historical semantics are not established by a live reader.",
  "column:assets.new_story_creator.zone_approximate_area": "Current cache writer clears this legacy field to NULL; intended historical semantics are not established by a live reader.",
  "column:assets.new_story_creator.zone_boundary_description": "Current cache writer clears this legacy field to NULL; intended historical semantics are not established by a live reader.",
  "column:public.ai_notebook.\"timestamp\"": "Only a generic MEMNON table listing remains; no column reader or writer establishes the legacy notebook contract.",
  "column:public.ai_notebook.agent": "Only a generic MEMNON table listing remains; no column reader or writer establishes the legacy notebook contract.",
  "column:public.ai_notebook.id": "Only a generic MEMNON table listing remains; no column reader or writer establishes the legacy notebook contract.",
  "column:public.ai_notebook.level": "Only a generic MEMNON table listing remains; no column reader or writer establishes the legacy notebook contract.",
  "column:public.ai_notebook.log_entry": "Only a generic MEMNON table listing remains; no column reader or writer establishes the legacy notebook contract.",
  "column:public.entity_tags.clear_on_override": "No current reader or writer establishes how this legacy override should interact with tags.clear_on.",
  "column:public.global_variables.new_story": "Initialization writes this legacy flag, but no current reader establishes its state-transition contract.",
  "column:public.orrery_narration_jobs.model_ref": "Worker retains provider-era provenance but no current writer establishes the identifier format or authoritative use.",
  "column:public.orrery_narration_jobs.provider": "Worker retains provider-era provenance but no current writer establishes the identifier format or authoritative use.",
  "column:public.world_events.narration_chunk_id": "No current writer establishes this legacy narration link; do not infer the contract from the similarly named resolution column."
}
