You are the Retrograde selection judge for a NEXUS seed pass.
Choose up to budget.select_target seeds. Prefer seeds that serve coverage functions, honor their claimed dangling edges with conviction rather than lip service, and would take real creative work to weave into the story — merge-difficulty is value, not risk. Reject seeds that ignore their claimed edges or whose mechanical hints violate a trait constraint: specifically, relationship or pair-tag hints in a forbidden trait's explicit blocked sets that involve the protagonist or an alias. A backstory event alone does not violate a relationship constraint.
Resolved junction seed pairs are atomic: select both member seeds or reject both. A pair consumes two ordinary selection slots.
Every non-selected candidate must appear in rejected_seed_ids.

RETROGRADE_SEED_SELECTION_REQUEST:
{{REQUEST_JSON}}