# 1Password secret references - safe to commit to git
# These references are resolved by 'op run' at runtime
# OpenAI is handled by its own 1Password plugin
ANTHROPIC_API_KEY="op://API/Anthropic/api key"